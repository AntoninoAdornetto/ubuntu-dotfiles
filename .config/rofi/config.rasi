@import "~/.config/rofi/root-config.rasi" 

/* ---- Configuration ---- */
configuration {
	font:	"JetBrains Mono 16";
}
/* ---- Window ---- */
window {
    width:	40%;
}

/* ---- Entry input ---- */
entry {
    width:	18%;
}

/* ---- Listview ---- */
listview {
    lines:	8;
}

/* ---- Mode Switcher ---- */
button {
    width:	110px;
}

element-text {
    font:	"JetBrains Mono 16";
}
